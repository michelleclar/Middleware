<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.carl.mybatis.mapper.UserMapper">
    <resultMap id="userResultMap" type="user">
        <!--主键-->
        <id column="id" property="id"></id>
        <!--普通字段-->
        <result column="username" property="password"></result>

    </resultMap>
    <delete id="deleteIds">
        delete
        from t_user
        where id in (${ids});
    </delete>


    <select id="selectAll" resultType="com.carl.mybatis.pojo.User">
        select *
        from t_user;
    </select>
    <select id="getCount" resultType="java.lang.Integer">
        select count(*)
        from t_user;
    </select>
    <!--返回自增主键-->
    <insert id="insertUser" useGeneratedKeys="true"  keyProperty="id">
        insert into t_user (id, username, password, gender, email)
        values (null,#{username},#{password},#{gender},#{email});
    </insert>


</mapper>